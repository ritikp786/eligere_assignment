Prerequisites:

- Node.js
- MySQL
- Browser

Steps to run :
1. Copy `.env.example` as `.env`
2. Add your credentials in the `.env` file
3. Run SQL commands from `table_migration.sql` 
4. Run `npm install` command 
5. Run `npm start`
6. Server will run on url `127.0.0.1:8080` by default.